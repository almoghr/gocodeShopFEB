{
  "name": "gocodeshopserver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "render-postbuild": "cd client && yarn install && yarn build"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "mongoose": "^7.2.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
